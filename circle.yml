machine:
  java:
    version: openjdk8

general:
  artifacts:
    - "build/reports"
test:
  pre:
    - sudo cp jce_policy-6/* $JAVA_HOME/jre/lib/security/
  override:
    - ./gradlew build uploadArchives -PsonatypeUsername=$SONATYPE_USERNAME -PsonatypePassword=$SONATYPE_PASSWORD
