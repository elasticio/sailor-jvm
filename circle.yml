general:
  artifacts:
    - "build/reports"
test:
  pre:
    - cp jce/* $JAVA_HOME/jre/lib/security/
  override:
    - ./gradlew build uploadArchives -PsonatypeUsername=$SONATYPE_USERNAME -PsonatypePassword=$SONATYPE_PASSWORD